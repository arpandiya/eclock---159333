spring:
  application:
    name: eclock
  devtools:
    restart:
      enabled: true
    livereload:
      enabled: true
  #  datasource:
#      url: jdbc:mysql://localhost:3306/eclock_mysql_db
#      driver-class-name: com.mysql.cj.jdbc.Driver
#      username: eclock_user
#      password:
  datasource:
    url: jdbc:postgresql://localhost:5432/eclock_postgres_db
    driver-class-name: org.postgresql.Driver
    username: eclock_user
    password: eclock@pass

  jpa:
    show-sql: true
    ddl-auto: create

    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
    generate-ddl: true

  thymeleaf:
    cache: false

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              basic:
                binder:
                  parameter: TRACE

